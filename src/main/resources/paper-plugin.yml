name: PageLore
version: '${version}'
main: net.danh.pagelore.PageLore
api-version: '1.21'
load: POSTWORLD
authors: [ VoChiDanh ]
folia-supported: true
dependencies:
  server:
    packetevents:
      load: BEFORE
      required: true
    PlaceholderAPI:
      load: BEFORE
      required: false